const Tasks = ({title, description, status}) => {
    return(
        <div>
            <div>
                <h1>{title}</h1>
                
            </div>
        </div>
    )
}

export default Tasks